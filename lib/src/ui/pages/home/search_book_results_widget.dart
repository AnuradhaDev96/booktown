import 'package:flutter/material.dart';

class SearchBookResultsWidget extends StatelessWidget {
  const SearchBookResultsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
